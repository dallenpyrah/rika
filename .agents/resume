#!/usr/bin/env bash
set -euo pipefail

export PATH="${BUN_INSTALL:-$HOME/.local}/bin:$PATH"

if ! command -v bun >/dev/null; then
  echo "Bun is unavailable; rerun .agents/setup" >&2
  exit 1
fi

echo "Rika orb ready (Bun $(bun --version)); no local services to reconnect"
