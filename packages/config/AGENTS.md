# @rika/config

Owns typed settings, deterministic global/workspace resolution, diagnostics, and the explicit application environment boundary. It does not read files or initialize providers, extensions, or UI infrastructure.
