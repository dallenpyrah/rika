# @rika/extensions

Owns local extension discovery and activation. It may depend on Baton public extension contracts but must not import Baton internals, application, runtime, TUI, tool, persistence, MCP, or plugin modules.
