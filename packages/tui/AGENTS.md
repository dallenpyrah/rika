# @rika/tui

Owns pure terminal state and the only OpenTUI adapter. It consumes product events and callbacks, never SQL, Relay, Baton, or provider APIs. The terminal background is inherited and renderer lifecycle is scoped.
