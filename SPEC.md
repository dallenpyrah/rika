# Rika Documentation

`PRODUCT.md` owns product direction. `CONTEXT.md` owns vocabulary and system boundaries. Current capability contracts live in `docs/features/`; lasting choices and real tradeoffs live in `docs/decisions/` and `docs/tradeoffs/`. Work tracking files do not define architecture or product behavior.
