# Command palette

The searchable command palette lets terminal users create a Thread, switch Thread, change mode, toggle fast mode, or quit. It opens as an exclusive overlay and shows each command's category and keybinding when one exists.

Typing filters commands, arrow keys move the selection, Enter runs the selected command, and Escape closes the palette. An unmatched query stays open and performs no action.
