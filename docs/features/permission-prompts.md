# Permission prompts

Explicit permission and tool-approval requests appear as actionable transcript cards containing a title, detail, and the choices `allow`, `always`, and `deny`. Users move between choices and confirm one for that specific request.

The chosen decision updates only the matching card and is sent with its permission kind and identifier. Ordinary tool events do not create permission prompts.
