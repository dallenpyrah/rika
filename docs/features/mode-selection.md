# Mode selection

Users choose one of the `low`, `medium`, `high`, or `ultra` behavior modes from the mode picker. The picker starts on the active mode, wraps during keyboard navigation, and applies the selected mode to later submissions.

Each mode supplies its configured reasoning effort and model route. Fast mode is a separate toggle; the terminal does not offer a direct reasoning-effort override.
