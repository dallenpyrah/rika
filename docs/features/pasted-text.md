# Pasted text

Short single-line pastes remain ordinary composer text. Multiline text, carriage-return text, and text longer than 120 characters collapse to a numbered pasted-text token so large pastes do not take over the composer.

Opening a token for editing restores the exact pasted text. Submission, transcript history, persistence, and model input receive the exact expanded text in its original position, never the display token.
