# Markdown rendering

Assistant Markdown renders headings with depth hierarchy and preserves inline emphasis, links, lists, code, and other supported block structure within the transcript's available display-cell width.

Tables render as bounded rounded grids with header and body separators. Cells wrap without truncating content; when a grid cannot fit, cells stack so all content remains visible.
