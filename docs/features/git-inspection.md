# Git inspection

Agents inspect the current branch, staged changes, unstaged changes, and untracked files through `bash`, using `git --no-optional-locks status --short --branch`. Git inspection is a normal shell command, not a separate tool.
