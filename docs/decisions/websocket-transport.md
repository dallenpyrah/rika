# WebSocket transport

Rika uses Effect Streams inside the resident and one authenticated typed WebSocket contract across the local process boundary. Bidirectional interaction, acknowledgements, bounded delivery, and reconnect fit one connection; SSE would split events from steering, permissions, and cancellation.
