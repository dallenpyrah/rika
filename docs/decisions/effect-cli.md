# Effect CLI

All argument-bearing commands use `effect/unstable/cli`, with business behavior behind Effect services. This keeps parsing, validation, output, and runtime requirements typed and testable without introducing a second command runtime.
