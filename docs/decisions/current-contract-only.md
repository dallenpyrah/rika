# One current contract

Rika is pre-1.0 and keeps one current schema, command, protocol, and projection shape. Disposable state is rebuilt instead of carrying compatibility decoders or parallel implementations because all current callers are controlled together.
