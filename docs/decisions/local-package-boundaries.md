# Local product and package boundaries

Rika ships as one local CLI and TUI for one owner. It consumes pinned released Baton and Relay packages through public exports only because hosted surfaces and copied framework code would add unrelated infrastructure and split ownership.
