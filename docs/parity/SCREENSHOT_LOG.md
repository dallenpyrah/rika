# Screenshot & recording log

Append-only running record of every Amp ⇄ Rika capture pair. Never delete rows;
when a delta is fixed, add a new row with fresh evidence rather than editing the
old one. This is the audit trail proving parity claims in `goal.md`.

Naming: `docs/parity/screenshots/{amp,rika}/<surface>-<state>-<NN>.png`
and `docs/parity/recordings/{amp,rika}/<surface>-<behavior>-<NN>.<ext>`.

Verdict: `match` · `mismatch` · `unverified`.

| # | Date | Surface | State / behavior | Amp file | Rika file | Verdict | Delta found / action |
| - | ---- | ------- | ---------------- | -------- | --------- | ------- | -------------------- |
| _ | _    | _       | _                | _        | _         | _       | _ (no captures yet)  |
