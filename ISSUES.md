# Rika ↔ Amp parity — open issues

Closed issue notes from the parity push. This file is not a product or architecture source.

## Bugs

- [x] 1. Cancelling a turn (Esc) surfaces `✖ ERROR: Execution failed — No active turn` instead of a clean `cancelled` notice. (Verified live: Ctrl+C mid-turn renders a single muted `cancelled` notice, no error block, stream halts.)
- [x] 2. Exiting rika hangs ~10s and spews `WARN … No healthy runners available (effect/cluster/Sharding)` to the terminal before the prompt returns. (Fixed rika-side with bounded hard-exit + upstream: relay entity-termination timeout 2s and the warning filtered on embedded single-node runtimes. Verified live: prompt back <3s, zero warnings.)
- [x] 3. No exit goodbye card. Amp prints logo + thread title + workspace + thread link + `amp threads continue <id>`; rika should print the equivalent (`rika threads continue <id>`). (Verified live: blue mini-mark card with workspace, thread title + `rika threads continue <id>` when a thread exists.)
- [x] 4. Responses render all at once — `model.output.delta` events are not reaching the TUI during the run (backend start/event pump, not a transport problem; vibe already streams via SSE).
- [x] 5. Attached images show as `[Pasted text #N]` and never materialize into image parts, so the model cannot see them. Pasted image paths must become `[Image #N]`-style attachments backed by real image prompt parts. (Verified: submitting a prompt with a bracketed PNG path produced a real `image/png` blob part in the `model.input.prepared` event; `insertPaste` → `[Image #N]` covered by unit tests.)
- [x] 6. Cmd+Backspace (delete to line start) and Opt+Backspace (delete previous word) do not work in the composer. (Verified live: Opt+Backspace deletes previous word, Cmd+Backspace/Ctrl+U delete to line start, Ctrl+W word.)
- [x] 7. Intermittent turn failure on parallel tool calls: `LanguageModel.streamText: Invalid output: Expected "find_files", got "shell" at [1][name]` — upstream (batonfx/effect-ai) strict decode should degrade to an error tool-result instead of failing the run. (Fixed upstream in BOTH loops: relay agent-loop retries with a corrective message, bounded by RELAY_AGENT_TOOL_CALL_DECODE_MAX_RETRIES; batonfx loop gains bounded corrective turns. Tests in both repos.)

## Deep-parity features

- [x] 8. Expandable tool sections (Opt+T / Tab navigation): expanded single-file `Edited` shows its diff directly, multi-file edits show independently expandable `Edit <path>` children, `Ran N commands` shows independently expandable `$ command` children with output and exact exit code, `Explored` lists per-file/search children, and subagents reveal their task. (Verified with reducer coverage and matched Amp Pilotty captures under `artifacts/transcript-parity/`.)
- [x] 9. Diff rendering via @pierre/diffs (line numbers, `-`/`+` colored rows, context ellipsis) for edit blocks and the review surface. (Verified: `renderPierreDiff` via `parsePatchFiles` renders numbered rows, red `-`/green `+`, dim context; falls back to the legacy renderer when a patch cannot be parsed.)
- [x] 10. Edit patches stream into the transcript while the model writes them. `model.toolcall.delta` updates the final semantic ToolCall row and its live per-file diff; request and result events update that same stable row. There is no draft row or second reducer.
- [x] 11. Thread Preview pane in Switch Thread must render the selected thread's actual transcript tail (grouped blocks), not metadata. (Verified live: preview pane renders the selected thread's grouped transcript tail — user prompt, Explored/Edited groups, assistant reply — via a debounced `previewThread` fetch projected through `projectTurn`/`renderTranscriptStyled`.)
- [x] 12. Right-side sidebar (command-toggled) showing a file tree of the workspace with changed files highlighted (git status). (Verified live: Opt+S opens a right sidebar tree colored by status with `+a -d` numstat counts; transcript shrinks while open.)
- [x] 13. Model-generated thread titles (amp uses a small model for titling; rika currently uses the first 80 chars of the prompt). Should update the tmux/terminal tab title too. (Verified live: after the first completed turn a LOW-mode background titling generated "Capital of France"/"Edit Greeting String"; terminal title set via OSC; titling errors are swallowed.)
- [x] 14. Shortcuts panel promises not yet implemented: Opt+D toggle reasoning effort, Ctrl+G edit prompt in $EDITOR, Tab/Shift+Tab navigate messages. (Verified live: Tab/Shift+Tab move detail selection, Opt+T toggles the selected group, Opt+D cycles reasoning effort with a toast + status label, Ctrl+G round-trips the composer through $EDITOR via renderer suspend/resume.)
- [x] 19. Mouse click on an expandable tool section (`✓ Explored … ▸`, `✓ Edited … ▸`, `$ cmd ▸`, `✓ Ran N commands ▸`, reasoning rows) must toggle its expansion, exactly like Opt+T on the selected block. Requires mapping the click row through the transcript scroll offset to the rendered block/group and dispatching the toggle. (Verified live: clicking the Edited row expanded it, clicking the `▾` header collapsed it, clicking the Explored row expanded its children — mapped via `lineInfo.lineSources` + scroll/pad offset to the unit's recorded line range; toggle owned by view-state.)
- [x] 20. Mode label color parity (amp): the mode name in the input-box title must render in the same color as the welcome orb for that mode (`colors[mode]`), not plain text. Fast mode is a separate toggle like amp's: add a command-palette entry to toggle it; when ON the title shows the `↯` prefix and the label renders yellow/gold instead of the mode color. The `↯` glyph indicates fast mode, not the mode itself — remove the current hardcoded `↯medium`/`↯high` rule. (Verified live: the mode name renders in `colors[mode]` on the input-box top border via a root-level styled overlay; the palette "toggle fast mode" flips `fastMode`, showing `↯<mode>` in gold `#f2c94c`; the old hardcoded `↯` rule removed.)

## Upstream (../batonfx, ../relay)

- [x] 15. batonfx: resilient handling of malformed/parallel tool calls (issue 7). (Done: batonfx corrective-turn resilience, 80 core tests green, dist rebuilt.)
- [x] 16. batonfx/relay: emit tool-call argument delta events so the TUI can stream patches (issue 10). Relay emits `model.toolcall.delta` `{tool_call_id, tool_name?, delta, delta_index}`; Rika folds it into the final ToolCall projection.
- [x] 17. relay: clean, fast runner/sharding shutdown — no `No healthy runners available` warning loop, no ~10s teardown hang (issue 2). (Done: entity termination timeout bounded to 2s on embedded runtimes, RunnerStorage-sync warning precisely filtered; cancellation test wall-time 15s → 4s.)
- [x] 18. relay: cancellation should resolve the active execution cleanly so clients see `cancelled`, not a follow-up error (issue 1). (Done: cancel path verified end-to-end upstream with a running-execution cancellation test; rika shows clean `cancelled`.)
- [x] 21. fastMode and reasoningEffort must take effect downstream: Opt+D's effort (and each mode's configured `reasoning`) must reach the model request (`reasoning_effort`), and fast mode must request priority serving (`service_tier: "priority"`). Plumb TUI → session → operation → backend start → per-effort/fast variant model registrations selected via `registration_key`; children inherit the parent selection. (Verified on the wire: Opt+D effort → `reasoning_effort: xhigh` and fast mode → `service_tier: "priority"` in the vibe request; each mode's configured `reasoning` now flows as the default — the background titling call went out as `low`. Implemented via per-effort/fast variant registrations selected through `registration_key`; no relay/baton changes required — their selection conversion already preserves the key.)
- [x] 22. Cross-turn conversation memory is missing: each thread turn starts a fresh relay execution whose chat contains only [system, new prompt] — verified on the wire (turn 2 sent msgs=2) and behaviorally (model answered "None" about the prior turn, resorting to find_thread). Relay's fresh-run path must rehydrate session history (baton already supports persisted chats / explicit history; relay's session store currently only feeds compaction). Rehydration must be deterministic/append-only so provider prompt caching keeps hitting across turns. (Fixed upstream: relay rehydrates session history via SessionRepository→buildContext→RunOptions.history, append-only for cache determinism; 3 new relay tests. Verified live: model recalled the prior turn's secret word 'orbital-otter'.)
- [x] 23. Multi-turn sessions stall: after several turns the run sticks at `∼ Streaming` and stops progressing. (Fixed: backend.start now races start-resolve vs stream-terminal and bounds the stream join with a replay-reconcile fallback, so a turn can never hang if the stream misses the terminal. Verified live: 6 turns incl. a full AGENTS.md dump all idled in 3-18s, no stall.)
- [x] 24. Mouse report fragments leak into the composer as text (`<65;111;8M…`) — SGR sequences are reaching the key path undecoded. (Fixed: SGR mouse reports are decoded/suppressed at the key boundary (full-sequence regex + burst buffering) so they never reach the composer. Verified live: input box clean after scroll+two clicks.)
- [x] 25. `Connecting` shows between model calls during a turn; it should only appear at startup. Use Waiting/Thinking/Streaming/Working during turns. (Fixed: mid-turn tool-result now shows 'Waiting' not 'Connecting'. Verified live: only 'Thinking' seen across a turn.)
- [x] 26. Click-to-expand does not work in a real terminal (worked only under pilotty's synthetic events) — likely same root cause as 24. (Fixed: click handler also bound on the scroll container with click-dedup; maps click row through scroll+pad offset to the unit. Verified live in a real terminal: clicking '✓ Explored 1 file ▸' expanded to '✓ Read PRODUCT.md', clicking again collapsed it.)
- [x] 27. Exit card layout differs from amp: thread title must sit on the logo's second row, workspace on the third (thread link row omitted — rika is local), and `rika threads continue <id>` must print as a standalone line below the card after a blank line, exactly like amp's `amp threads continue <id>`. (Fixed: title on logo row 2, workspace row 3, 'rika threads continue <id>' as a standalone dim line below; current-thread id/title now tracked on the model via ThreadActivated so the card populates. Verified live in shell scrollback.)
